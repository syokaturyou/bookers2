
    <% if book.favorited_by?(current_user) %>
        <p><%= link_to "#{book.favorites.count}", book_favorites_path(book.id), method: :delete, remote: true, class:"fas fa-heart", :style => "color: red;" %><p>
    <% else %>
        <p><%= link_to "#{book.favorites.count}", book_favorites_path(book.id), method: :post, remote: true, class:"fas fa-heart", :style => "color: royalblue;" %></p>
    <% end %>





